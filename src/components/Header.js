import SwitchMode from "./SwitchMode";

const Header = () => {
    return (
        <header>
            <p>Where in the world</p>
            <SwitchMode />
        </header>
    );
}
 
export default Header;